@model JW_Management.Models.Paragem
@{
    ViewData["Title"] = "Paragem";
}

<form asp-action="Index" method="get" id="frmFiltrar">
    <div class="container">
        <div class="columns">
            <div class="column">
                <div class="tile">
                    <div class="tile is-parent is-vertical">
                        <article class="tile is-child notification is-info">
                            <p class="title">
                                <span class="icon mr-3">
                                    <i class="fa-solid fa-newspaper"></i>
                                </span>
                                <span>@Model.Tipo.Nome - @Model.NomeParagem</span>
                            </p>
                        </article>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>
<hr />

<div class="container">
    @if (Model.Viagens.Count() == 0)
    {
        <br />
        <button class="button is-danger is-large is-rounded is-fullwidth is-disabled">
            Não foram encontradas
            viagens!
        </button>
    }
    else
    {
            <div class="columns is-gapless">
                <div class="column mx-1">
                    <div class="card has-table has-mobile-sort-spaced">
                        <header class="card-header has-background-success">
                            <p class="card-header-title" style="color:#ffffff">Viagens</p>
                            <button class="button is-pulled-right m-2">@Model.Viagens.Count()</button>
                        </header>
                        <div class="card-content">
                            <div class="b-table">
                                <div class="table-wrapper has-mobile-cards">
                                    <table class="table is-hoverable is-fullwidth">
                                        <thead>
                                        <tr>
                                            <th>
                                                @Html.DisplayNameFor(model => model.Viagens.FirstOrDefault()!.Viagem.Rota)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.Viagens.FirstOrDefault()!.Viagem.Destino)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.Viagens.FirstOrDefault()!.DataPartida)
                                            </th>
                                            <th></th>
                                        </tr>
                                        </thead>
                                        <tbody>
                                        @foreach (var item in Model.Viagens.OrderBy(v => v.DataPartida))
                                        {
                                            <tr>
                                                <td data-label="@Html.DisplayNameFor(model => model.Viagens.FirstOrDefault()!.Viagem.Rota)">
                                                    <span>@item.Viagem.Rota.Nome</span>
                                                </td>
                                                <td data-label="@Html.DisplayNameFor(model => model.Viagens.FirstOrDefault()!.Viagem.Destino)">
                                                    <span>@item.Viagem.Destino</span>
                                                </td>
                                                <td data-label="@Html.DisplayNameFor(model => model.Viagens.FirstOrDefault()!.DataPartida)">
                                                    <span>@item.DataPartida.ToShortTimeString()</span>
                                                </td>
                                                <td class="is-actions-cell">
                                                    <a class="button is-info is-fullwidth" href="@(Url.Action("Viagem", "Congressos", new { id = item.Viagem.Id, IdIC = @Model.Congresso.Id }))">Ver Viagem</a>
                                                </td>
                                            </tr>
                                        }
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
    }
</div>
